package br.com.alfaschool.backend.domain.access.shared;

/** ABERTA: aluno dentro da escola. INCONSISTENTE: par entrada/saida impar ou incoerente — nao entra em nenhum total. */
public enum StatusPresenca { ABERTA, FECHADA, INCONSISTENTE, AJUSTADA }
