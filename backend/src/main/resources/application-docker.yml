spring:
  datasource:
    url: jdbc:mysql://mysql:3306/alfaschool?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
    username: root
    password: ${MYSQL_ROOT_PASSWORD:alfaschool123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: redis
      port: 6379

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost,http://localhost:80}
